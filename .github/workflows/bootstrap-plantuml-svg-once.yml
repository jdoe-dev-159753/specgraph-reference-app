name: bootstrap-plantuml-svg-once

on:
  push:
    paths:
      - '.github/workflows/bootstrap-plantuml-svg-once.yml'

permissions:
  contents: write

jobs:
  regenerate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ci/plantuml-render-freshness
          fetch-depth: 1
      - name: Regenerate controlled SVG corpus with the permanent renderer
        run: bash scripts/render_plantuml.sh
      - name: Commit generated SVG views
        run: |
          git status --short -- docs/assignment
          git add -- 'docs/assignment/**/*.svg'
          if git diff --cached --quiet; then
            echo 'All controlled SVGs are already current.'
            exit 0
          fi
          git diff --cached --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'docs: regenerate controlled PlantUML SVG views'
          git push origin HEAD:ci/plantuml-render-freshness
