name: ops-force-cancel-reboot-zombie

on:
  push:
    branches:
      - ops/cancel-stuck-r5-34236806824

permissions:
  actions: write
  contents: read

jobs:
  cancel:
    runs-on: ubuntu-latest
    steps:
      - name: Force-cancel reboot-zombie application CI
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api --method POST "repos/${{ github.repository }}/actions/runs/34236806864/force-cancel" || true
