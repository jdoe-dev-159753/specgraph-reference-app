name: temp-render-sdd-semantics

on:
  workflow_dispatch:
  push:
    branches:
      - docs/sdd-readable-figures
    paths:
      - 'docs/assignment/SDD/diagrams/**.puml'
      - 'docs/assignment/SDD/diagrams/**.dot'
      - '.github/workflows/temp-render-sdd-semantics.yml'

permissions:
  contents: write

jobs:
  render:
    runs-on: [self-hosted, linux, x64, specgraph-reference-app, ci, docker]
    steps:
      - uses: actions/checkout@v7
        with:
          ref: docs/sdd-readable-figures
          persist-credentials: true
      - name: Render PlantUML sources
        shell: bash
        run: |
          docker run --rm \
            --user "$(id -u):$(id -g)" \
            -v "$PWD:/workspace" \
            -w /workspace \
            plantuml/plantuml:1.2026.6 \
            -tsvg docs/assignment/SDD/diagrams/*.puml
      - name: Render Graphviz delivery rings
        shell: bash
        run: |
          docker run --rm \
            --user "$(id -u):$(id -g)" \
            --entrypoint dot \
            -v "$PWD:/workspace" \
            -w /workspace \
            plantuml/plantuml:1.2026.6 \
            -Tsvg docs/assignment/SDD/diagrams/delivery-rings.dot \
            -o docs/assignment/SDD/diagrams/delivery-rings.svg
      - name: Commit rendered SVGs
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/assignment/SDD/diagrams/*.svg
          git diff --cached --check
          if git diff --cached --quiet; then
            echo "No rendered changes"
            exit 0
          fi
          git commit -m "docs: render final SDD figures"
          git push
