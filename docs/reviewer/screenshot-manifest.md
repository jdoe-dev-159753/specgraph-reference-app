# Screenshot promotion manifest

The first screenshot promotion pass should use authentic CI/browser evidence only.

Known captures already retained outside the repository and suitable for promotion once copied into a repository-owned evidence path:

- R1 customer `11111111-1111-1111-1111-111111111111` from application-ci #329;
- R2 customer `22222222-2222-2222-2222-222222222222` from application-ci #329;
- R3 customer `33333333-3333-3333-3333-333333333333` from application-ci #329;
- deterministic R4 customer `44444444-4444-4444-4444-444444444444` from complete-flow run `33653841308`.

The root README should embed these only after the binary files themselves are present in the repository or another durable reviewer-facing publication. Do not link ephemeral sandbox paths or expiring workflow artifact URLs.

Future captures should add R4 Bayesian and, if implemented, R4 local-model variants so the README can show the same input surface with different detector/backend provenance.
