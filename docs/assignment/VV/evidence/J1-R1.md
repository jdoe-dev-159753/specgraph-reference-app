# J1 / R1 executable evidence provenance

This record identifies one concrete J1 R1 verification execution. It is provenance for generated evidence, not a hand-maintained current-status authority. Stable verification obligations remain in [`../verification.yaml`](../verification.yaml) and the verification strategy remains in [`../VV.md`](../VV.md).

## Tested revision

- PR: `#66` — `feat: add verifiable mandatory R1 customer review`
- exact PR head verified: `009b9b7337ced7632b91c0be7e6e9bd05614687b`
- merge commit on `main`: `012d6663be8a6dc9edc6d2d2427f3d653de6bccb`
- stable demonstration branch after merge: `demo/r1`

GitHub's `pull_request` workflow context also produced the synthetic merge-ref SHA `4a97c30e53e3a6a52047d09919ae0a35c11df669`; that value appears in the generated artefact name. The workflow-run metadata independently records the actual tested head SHA above.

## Verification execution

- workflow: `application-ci`
- Actions run: [`33404580943`](https://github.com/jdoe-dev-159753/specgraph-reference-app/actions/runs/33404580943)
- verification job: `99528855440`
- run scenario:
  1. clean container-owned build outputs;
  2. backend `mvn verify`;
  3. frontend build;
  4. Docker Compose topology validation;
  5. clean Compose startup;
  6. backend actuator + frontend availability checks;
  7. Playwright browser execution against `http://127.0.0.1:5173`;
  8. API-response and visible-UI assertions for seeded customer, CARD/PAYMENT/CRYPTO activity and risk evidence;
  9. explicit unknown-customer 404 assertion;
  10. Compose status/log capture, evidence upload and clean shutdown.

The successful run exercises the methods required by `VFY-CUSTOMER-READ-001` (`api_acceptance`, `ui_e2e`) and the J1 portion of `VFY-REPRODUCIBILITY-001` (`clean_checkout_compose`, `smoke_acceptance`).

## Generated artefact

- Actions artifact ID: `9762799985`
- name: `r1-executable-evidence-4a97c30e53e3a6a52047d09919ae0a35c11df669`
- size: `2,931,140` bytes
- digest: `sha256:e0018fffd9bcc20960696174bbaf8d2c8fec245c706dde816e33f6404dd48741`
- created: `2026-08-31T14:51:48Z`
- configured expiry: `2026-09-30T14:51:46Z`
- artifact API identity: <https://api.github.com/repos/jdoe-dev-159753/specgraph-reference-app/actions/artifacts/9762799985>

The uploaded evidence contains the Playwright HTML report/trace and test-results directory together with captured Compose status/logs. Artifact retention is deliberately finite; reproducibility comes from the versioned workflow and stable `demo/r1` checkpoint, while the run ID, tested SHA and cryptographic artifact digest preserve the provenance of this accepted execution.

## Local reproduction

```bash
git fetch origin
git switch demo/r1
docker compose up
```

Open <http://localhost:5173> and search for:

```text
11111111-1111-1111-1111-111111111111
```

The browser should expose deterministic CARD, PAYMENT and CRYPTO rows and their risk evidence. An unknown UUID should return the explicit not-found behaviour. No database assertion belongs to this record because R1 intentionally uses `SyntheticActivityAdapter`; relational persistence is activated in a later ring.
