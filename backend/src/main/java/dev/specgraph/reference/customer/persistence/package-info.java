/**
 * Outbound JPA adapters for customer activities and source risk evidence.
 *
 * <p>Persistence entities are translated into the immutable customer model before crossing the
 * application port; database and JPA types do not escape this adapter package.
 */
package dev.specgraph.reference.customer.persistence;
