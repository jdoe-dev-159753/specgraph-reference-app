/**
 * Analysis application module: orchestration, inbound use cases, outbound ports and
 * provider-neutral evidence/result types.
 *
 * <p>Adapters implement the ports declared here. Model, retrieval, persistence and detector
 * technologies must depend on these contracts; the contracts must not expose provider types.
 */
package dev.specgraph.reference.analysis;
