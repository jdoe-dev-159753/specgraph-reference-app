/**
 * Application-owned operator identity and authentication-context contracts.
 *
 * <p>Security and transport adapters translate framework principals into these explicit context
 * variants so accountability semantics remain independent of the authentication provider.
 */
package dev.specgraph.reference.identity;
