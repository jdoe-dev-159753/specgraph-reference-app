/**
 * Spring Security configuration and operator-context adapter.
 *
 * <p>Framework authentication state is interpreted here and exposed inward through the
 * application-owned identity port; security-provider types do not cross that boundary.
 */
package dev.specgraph.reference.identity.security;
