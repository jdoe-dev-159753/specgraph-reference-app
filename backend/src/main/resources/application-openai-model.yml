spring:
  ai:
    model:
      chat: openai
      embedding: none
      image: none
      moderation: none
      audio:
        speech: none
        transcription: none
    openai:
      api-key: ${OPENAI_API_KEY}
      timeout: 20s
      max-retries: 1
      chat:
        model: ${OPENAI_MODEL:gpt-5-mini}
