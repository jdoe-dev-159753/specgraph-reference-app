# CI overlay binds R5 to pre-provisioned network/cache resources so the proof remains hermetic and reusable.
networks:
  default:
    external: true
    name: "${R5_NETWORK_NAME}"

volumes:
  r5-embedding-cache:
    external: true
    name: specgraph-r5-ci-embedding-cache
